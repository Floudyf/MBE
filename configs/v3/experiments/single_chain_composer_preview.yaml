profile_type: experiment_profile
profile_id: single_chain_composer_preview
version: v3
stage: v3.3.2

experiment:
  experiment_id: v3_3_2_single_chain_composer_preview
  stage: v3.3.2
  type: single_chain_composer_preview
  truth_label: modular_runtime
  backend_type: modular_research_chain
  runtime_mode: preview_only
  runnable: false

chain_profile: single_chain_research_default
experiment_template: metatrack_ablation

composer:
  enabled: true
  view: single_chain
  module_graph_id: single_chain_default

variable_modules:
  - Routing
  - Execution
  - StateAccess
  - Commit

fixed_modules:
  - Workload
  - TxPool
  - BlockProducer
  - Consensus
  - StateStorage

planned_modules:
  - CommitteeEpoch

plugin_profiles:
  baselines:
    - baseline_hash_only
    - co_access_only
    - co_access_dual_track
  proposed:
    - full_MetaTrack

workload:
  source: synthetic
  tx_count: 100
  seed: 42
  submit_rate: 200
  arrival_sequence: deterministic_seed_42
  key_count: 32
  hot_key_count: 4
  hotspot_ratio: 0.25
  skew: 1.0
  access_list_enabled: true
  aggregation_candidates_enabled: true

calibration:
  enabled: false
  calibration_profile_id: none
  source: none
  required_for_all_baselines: false

fairness:
  same_workload: true
  same_seed: true
  same_tx_count: true
  same_chain_profile: true
  same_hardware_profile: true
  same_submit_rate: true
  same_block_config: true
  same_consensus_config: true
  same_network_profile: true
  same_calibration_profile: true
  only_plugin_diff: true
  allowed_plugin_diff_classes:
    - ShardingPlugin
    - ExecutionSchedulerPlugin
    - StateAccessPlugin
    - CommitPlugin

outputs:
  expected:
    - composer_preview.json

capability:
  status: planned
  min_stage: v3.3.2
  backend_type: modular_research_chain
  runnable: false
  blocking_reasons:
    - preview-only composer profile; no runtime execution path
