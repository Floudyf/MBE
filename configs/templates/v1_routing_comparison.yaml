name: v1_routing_comparison
stage: v1.1
fixed_components: {workload: asset_hotspot, trace: jsonl_gzip, runtime: virtual_clock, execution: serial_execution, commit: normal_commit}
replaceable_components: [routing]
required_trace_fields: [tx_id, timestamp, access_list, status]
output_metrics: [throughput_tps, p99_latency_ms, remote_fetch_count]
runnable: false
description: Planned routing comparison template; alternative routing is not implemented in V1.1.
