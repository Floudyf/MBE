experiment: {name: v1_ablation_no_routing, version: v1, stage: v1.1, runnable: false, implemented: false, description: Planned no-routing ablation declaration only., seed: 42}
template: v1_ablation
workload: {plugin: asset_hotspot}
trace: {plugin: jsonl_gzip}
runtime: {clock_mode: virtual}
routing: {plugin: hash_routing}
execution: {plugin: dual_track_execution}
commit: {plugin: hot_update_aggregation}
metrics: {plugin: basic_metrics}
output: {dir: experiments/runs/v1_ablation_no_routing}
