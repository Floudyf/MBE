# Configurations

`experiments/` contains reproducible V0 input plus V1 declarations. Only `v1_baseline_hash_serial.yaml` is runnable; V1.3 trace-generation declarations remain planned in the Composer. `templates/` contains V1.1 component-replacement templates, `plugins/` declares the permitted default composition and each plugin's minimal `plugin.yaml`, and `schemas/` is reserved for future validation schemas.
