# Configurations

`experiments/` contains reproducible V0 input and V1.1 declarations. Only `v1_baseline_hash_serial.yaml` is runnable; every other V1 declaration is explicitly planned. `templates/` contains V1.1 component-replacement templates, `plugins/` declares the permitted default composition and each default plugin's minimal `plugin.yaml`, and `schemas/` is reserved for future validation schemas.
