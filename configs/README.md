# Configurations

`experiments/` contains reproducible V0 experiment input, `plugins/` declares the permitted default composition and each default plugin's minimal `plugin.yaml`, and `schemas/` is reserved for future V0 validation schemas.
