# Test-only sample

`dcl_sales_sample_for_test_only.csv` contains three synthetic-shaped records for
tests only (`sample_for_test_only=true`). It is not a Decentraland source
subset, is not used as provenance evidence, and must not be used for formal
results.
