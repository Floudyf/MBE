# Golden trace fixture

Run `python tests/golden/generate_trace_small.py` from the repository root to regenerate `trace_small.jsonl.gz` and `trace_meta.json`. The fixture contains two valid V0 single-chain records.
