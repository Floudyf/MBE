# asset_hotspot

Generate a deterministic V0 synthetic trace with `python -m workload.asset_hotspot.cli --config configs/experiments/v0_default_asset_hotspot.yaml --output experiments/runs/v0_default_asset_hotspot`. Records are written one at a time through the shared gzip trace writer.
