# Executor

Reserved for the Go 1.24 V0 replay executor. It will compose only the declared default plugins and calculate model latency with virtual time; it must not use `time.Sleep` for simulated latency.
