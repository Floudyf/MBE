# V1.4-b minimal chaincode

`asset`, `scene`, and `reward` are source-only Go Fabric contract skeletons. Their runner-facing functions align with `../access_schema.yaml`; future deployment reuses official `fabric-samples/test-network`. They do not start Fabric, emit raw JSONL, or enter default CI.
