# Scene chaincode
JoinScene aligns with `scene:`, `avatar:`, and `scene_member:` templates and emits `SceneJoined`. It rejects a duplicate membership so `MemberCount` only counts unique avatars. Source only; no Fabric is run.
